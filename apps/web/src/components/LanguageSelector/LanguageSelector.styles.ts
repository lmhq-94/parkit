import { SxProps, Theme } from '@mui/material';

export const languageSelectorStyles = {
  iconButton: {
    mr: 1,
  } as SxProps<Theme>,

  flagIcon: {
    fontSize: '1.2rem',
  } as React.CSSProperties,
};
