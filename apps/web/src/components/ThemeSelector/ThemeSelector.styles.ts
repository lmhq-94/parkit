import { SxProps, Theme } from '@mui/material';

export const themeSelectorStyles = {
  iconButton: {
    mr: 1,
  } as SxProps<Theme>,

  themeIcon: {
    fontSize: '1.2rem',
  } as React.CSSProperties,
};
