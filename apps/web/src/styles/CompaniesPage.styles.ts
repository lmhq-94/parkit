import { SxProps, Theme } from '@mui/material';

export const companiesPageStyles = {
  container: {
    p: 3,
  } as SxProps<Theme>,

  title: {
    mb: 3,
  } as SxProps<Theme>,

  // Add more styles as needed
};
