{
  "name": "parkit",
  "version": "1.0.0",
  "description": "Sistema Inteligente de Gestión de Parqueos - Multiplataforma",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:backend": "turbo run dev --filter=@parkit/backend",
    "dev:web": "turbo run dev --filter=@parkit/web",
    "dev:mobile": "turbo run dev --filter=@parkit/mobile",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint:fix",
    "format": "prettier --write \"**/*.{ts,tsx,md,json,js,jsx}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,md,json,js,jsx}\"",
    "check-types": "turbo run check-types",
    "test": "turbo run test",
    "test:watch": "turbo run test:watch",
    "test:coverage": "turbo run test:coverage",
    "clean": "turbo run clean",
    "db:generate": "turbo run db:generate --filter=@parkit/backend",
    "db:migrate": "turbo run db:migrate --filter=@parkit/backend",
    "db:seed": "turbo run db:seed --filter=@parkit/backend",
    "db:studio": "turbo run db:studio --filter=@parkit/backend",
    "storybook": "turbo run storybook --filter=@parkit/ui",
    "build:storybook": "turbo run build:storybook --filter=@parkit/ui",
    "prepare": "husky install"
  },
  "dependencies": {
    "scheduler": "^0.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "prettier": "^3.1.0",
    "turbo": "^1.11.0",
    "typescript": "^5.3.0",
    "husky": "^8.0.3",
    "lint-staged": "^15.2.0",
    "@commitlint/cli": "^18.4.0",
    "@commitlint/config-conventional": "^18.4.0"
  },
  "engines": {
    "node": ">=18.17.0",
    "yarn": ">=1.22.0"
  },
  "packageManager": "yarn@1.22.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "keywords": [
    "parking",
    "management",
    "react",
    "react-native",
    "nextjs",
    "graphql",
    "typescript",
    "turbo",
    "monorepo"
  ],
  "author": "ParkIt Team",
  "license": "MIT",
  "lint-staged": {
    "*.{ts,tsx,js,jsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md}": [
      "prettier --write"
    ]
  }
}
